import { Col } from "react-bootstrap";

const Notification = () => {
  return (
    <Col md={{ span: 8, offset: 2 }}>
      <p>You can get inspired by getting a single Quote every day.</p>
    </Col>
  );
};

export default Notification;
